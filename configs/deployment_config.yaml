server:
  host: "0.0.0.0"
  port: 8000

logging:
  level: "INFO"

artifacts:
  als: "artifacts/models/als"
  content: "artifacts/models/content"
  hybrid: "artifacts/models/hybrid"
  mappings: "data/processed/mappings"

cors:
  allowed_origins:
    - "http://localhost:3000"
    - "http://127.0.0.1:3000"
